import React, { useState } from 'react';
import { login } from '../services/api.js';

const LoginPage = () => {
    return (
        <div>
            <form onSubmit={handleLogin}>

            </form>
        </div>
    )
}